using System;
using System.Net.Sockets;

namespace Miner
{
    internal class MessageParser

    {
        public static NetworkMessage Parse(NetworkStream stream)
        {
            throw new NotImplementedException();
        }
    }
}